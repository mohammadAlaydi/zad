// Deprecated duplicate of operator.tsx — kept only as a redirect because
// the workspace did not allow deleting the file. Safe to remove manually.
import { Redirect } from "expo-router";

export default function OperatorFixedRedirect() {
  return <Redirect href="/bills/operator" />;
}
